TODO
  o Handle zero-time logs (logs where time of first and last query is the same)
  o Add version command/option
  o Get rid of Thor or create better integration for #error and #fail
  o Make sure replay doesn't include DELETE/INSERT/etc. statements
  o Document handling of double spaces in SQL
  o Make it possible to add comments to slow queries
  o QueryLogTools::Log#new vs. MysqlLogTools::parse <- do it the same way

  + Add ';' to all 'queries' commands

